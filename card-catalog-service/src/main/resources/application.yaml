server:
  port: 8082

spring:
  application:
    name: card-catalog-service

aws:
  dynamodb:
    endpoint: http://localhost:8000
  region: us-east-1
  accessKeyId: fake-access-key
  secretKey: fake-secret-key
