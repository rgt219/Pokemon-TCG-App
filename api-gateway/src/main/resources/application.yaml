server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: market-pricing-route
              uri: http://localhost:8081
              predicates:
                - Path=/api/market/**
